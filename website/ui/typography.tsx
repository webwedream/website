'use client';
import { Typography, TypographyProps } from '@material-tailwind/react';
import React from 'react';

export function LWTypography(props:TypographyProps) {
return (
  <Typography {...props} />
);
}